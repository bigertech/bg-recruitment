<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>职位管理</title>

  <link rel="stylesheet" href="{{asset '/stylesheets/bootstrap.min.css'}}">
  <link href="{{asset '/stylesheets/bootstrap-responsive.min.css'}}" rel="stylesheet">
  <link rel="stylesheet" href="{{asset '/stylesheets/style.css'}}">
  <link href="http://netdna.bootstrapcdn.com/font-awesome/3.0.2/css/font-awesome.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <div class="hero-unit">
    <form class="horizontal-form" enctype="multipart/form-data" role="form" method="POST" action="/admin/create_member">
      <div class="form-group">
        <label class="form-label">成员姓名</label>
        <input type="text" class="form-control" name="name" id="name" required>
      </div>
      <div class="form-group">
        <label class="form-label">成员职称</label>
        <input type="text" class="form-control" name="title" id="title" required>
      </div>
      <div class="form-group">
          <label class="form-label">成员箴言</label>
          <textarea style="width:600px;height:80px;" class="form-control" name="maxim" id="maxim"></textarea>
      </div>
      <div class="form-group">
          <label class="form-label">成员头像</label>
          <input class="form-control" type="file" name="img" />
      </div>
      <button id="create-member" type="submit" class="btn btn-primary form-btn-bar">确定</button>
      <a href="/admin/member" class="btn btn-primary form-btn-bar">返回</a>
    </form>
  </div>
</div>
<script src="{{asset 'javascripts/libs/jquery-1.10.2.min.js'}}"></script>
<script src="{{asset 'javascripts/libs/jquery.hotkeys.js'}}" type="text/javascript"></script>
<script>
  $(function () {
    $('#create-member').click(function() {
        var name = $('#name').val().trim();
        var title = $('#title').val().trim();

        if (name == '') {
            alert('「成员姓名」不能为空！');
            return false;
        } else if (title == '') {
            alert('「成员职称」不能为空！');
            return false;
        }

        return true;
    });
  });
</script>
</body>
</html>
